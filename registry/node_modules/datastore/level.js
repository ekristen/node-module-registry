var config = require('config')
var level = require('level')
var sublevel = require('sublevel')

var leveldb = sublevel(level(config.datastore.level.path, { valueEncoding: 'json', keyEncoding: 'utf8' }))

var db = leveldb.sublevel('packages')

module.exports = db
