var config = require('rc')('registry', {
  host: '0.0.0.0',
  port: 3000,
  auth: {
    type: 'none',
    github: {
      host: 'github.com',
      cache: {
        path: './data/github-cache'
      }
    }
  },
  datastore: {
    type: 'level',
    level: {
      path: './data/db'
    }
  },
  storage: {
    type: 'local',
    local: {
      path: './data/storage'
    },
    s3: {
      accessKeyId: null,
      secretAccessKey: null,
      region: null,
      bucket: null,
      prefix: null,
      redirect: false,
      expires: 60
    }
  },
  logger: {
    level: 'debug'
  },
  allow_previous_version: false
})

module.exports = config

var logger = require('logger')
logger.debug({config: config}, 'configuration')
