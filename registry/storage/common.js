class CommonStorage {
  getFile (file, callback) {
    callback();
  }

  saveFile (file, callback) {
    callback();
  }

  deleteFile (file, callback) {
    callback();
  }
}

module.exports = CommonStorage;
